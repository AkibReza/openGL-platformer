#pragma once

// Window settings
const unsigned int SCR_WIDTH = 800;
const unsigned int SCR_HEIGHT = 600;

// Game physics
const float GRAVITY = 0.00002f;        // Reduced from 0.0005f
const float JUMP_FORCE = 0.008f;      // Reduced from 0.015f
const float MOVEMENT_SPEED = 0.003f;  // Reduced from 0.005f

// Game state
const float LEVEL_END_X = 4.0f; 