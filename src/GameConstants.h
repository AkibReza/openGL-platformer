#pragma once

// Window settings
const unsigned int SCR_WIDTH = 800;    // Increased from 1024 to 1280 for wider view
const unsigned int SCR_HEIGHT = 800;    // Keeping height the same

// Game physics
const float GRAVITY = 0.00002f;         
const float JUMP_FORCE = 0.005f;       
const float MOVEMENT_SPEED = 0.0005f;   

// Game state
const float LEVEL_END_X = 2.0f;         // Keeping level length the same