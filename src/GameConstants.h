#pragma once

// Window settings
const unsigned int SCR_WIDTH = 1280;    // Increased from 1024 to 1280 for wider view
const unsigned int SCR_HEIGHT = 768;    // Keeping height the same

// Game physics
const float GRAVITY = 0.00001f;         
const float JUMP_FORCE = 0.0015f;       
const float MOVEMENT_SPEED = 0.0002f;   

// Game state
const float LEVEL_END_X = 2.0f;         // Keeping level length the same