#pragma once

// Window settings
const unsigned int SCR_WIDTH = 800;
const unsigned int SCR_HEIGHT = 600;

// Game physics
const float GRAVITY = 0.00002f;         // Keep gravity the same
const float JUMP_FORCE = 0.003f;        // Keep jump force the same
const float MOVEMENT_SPEED = 0.0002f;   // Reduced from 0.0005f to 0.0002f for slower movement

// Game state
const float LEVEL_END_X = 4.0f;